name: blast_env
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - usher
  - entrez-direct
  - blast
  - python
  - ncbi-datasets-cli
  - mafft
  - pip
  - pip:
      - taxoniumtools
